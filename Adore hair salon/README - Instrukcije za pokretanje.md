## Pokretanje aplikacije

1. Pokrenuti `schema.sql` i `seed.sql` u bazi podataka  
2. U terminalu pokrenuti:
   ```
   npm install
   npm run dev
   ```

Aplikacija se pokreće na `http://localhost:5173`, a API server na `http://localhost:3000`.
